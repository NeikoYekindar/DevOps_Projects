jenkins:
  persistence:
    enabled: true
    storageClass: "gp3"  # Change to "local-path" for K3s/K3d or your cluster's storage class
    size: "8Gi"
    # existingClaim: jenkins-server
  controller:
    jenkinsUrlProtocol: https
    jenkinsUrl: https://jenkins.neikoscloud.net
    JCasC:
      configScripts:
        welcome-message: |
          jenkins:
            systemMessage: "Welcome to the Production Jenkins Server"
      security: |
        jenkins:
          securityRealm:
            github:
              githubWebUri: "https://github.com"
              githubApiUri: "https://github.com/api/v3"
               
